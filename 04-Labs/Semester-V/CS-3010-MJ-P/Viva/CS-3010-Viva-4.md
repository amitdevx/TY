[[CS-3010-Assignment-4|Back to Assignment]]

# Viva Questions: MongoDB Aggregation Framework

1. What is the purpose of the MongoDB Aggregation Pipeline?
2. Explain the `$match` stage. Why is it recommended to place `$match` early in the pipeline?
3. How does the `$group` stage differ from `$project`?
4. What is the role of accumulators like `$sum` and `$avg` in the `$group` stage?
5. Compare MongoDB aggregation with SQL `GROUP BY`.
6. What is the limit on document size returned by an aggregation pipeline?
