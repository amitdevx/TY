[[CS-3010-Assignment-3|Back to Assignment]]

# Viva Questions: MongoDB CRUD Operations

1. Explain the difference between `insertOne()` and `insertMany()`.
2. What does the `find()` method return in MongoDB?
3. How does the `$set` operator work in an update operation?
4. What happens if you run `db.collection.updateMany({}, { $set: { active: true } })`?
5. How can you delete a database in MongoDB?
6. What is the `_id` field in a MongoDB document?
